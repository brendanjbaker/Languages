# Program

* Program name: `hello`

## Description

This program simply prints the message `Hello, world!`, to standard output, and exits with status `0`. The message should have a trailing newline (`\n`).

This program may seem pointless, however it serves as a useful first exercise in ensuring the language's toolchain is installed and working correctly.
