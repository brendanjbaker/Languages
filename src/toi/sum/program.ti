func sum_range(int from, int to) -> int:
{
	int sum = 0;
	int i = from;
	int to_exclusive = to + 1;

	while i < to_exclusive:
	{
		sum = sum + i;
		i = i + 1;
	}

	return sum;
}

func main -> void:
{
	print sum_range(0, 100);
}

main();
