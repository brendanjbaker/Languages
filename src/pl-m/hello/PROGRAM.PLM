0100H:

MON1: PROCEDURE(FUNC, PARM) EXTERNAL;
	DECLARE FUNC BYTE, PARM ADDRESS;
END MON1;

MAIN: PROCEDURE;
	DECLARE MESSAGE DATA ('Hello, world!', 10, '$');
	CALL MON1(9, .MESSAGE);
END MAIN;

CALL MAIN;
