(ql:quickload :coalton :silent t)

(cl:defpackage #:program
	(:use
		#:coalton
		#:coalton-prelude)
	(:export #:main))

(cl:in-package #:program)

(coalton-toplevel
	(define (main) Unit
		(progn
			(println (fold + 0 (range 0 101)))
			Unit)))

(coalton (main))
